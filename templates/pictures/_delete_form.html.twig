<form method="post" action="{{ path('app_pictures_delete', {'id': picture.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cette photo ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
    <button class="btn">Supprimer</button>
</form>
